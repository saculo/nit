# PRD — source

The v2 PRD this project was built from is not present in the repository; it was supplied to
`nit:clarify` during PHASE-2 planning and only its resolved output was persisted.

What survives verbatim is `.nit/CLARIFICATIONS.md`, the v1 clarification record, whose 24 items were
migrated into `summary.json` by TASK-032 with their counts verified against the source. That file is
retained unmodified as the historical record.

This placeholder exists so the path `nit:clarify` documents is present and its absence is explained,
rather than leaving a dangling reference. It is not a reconstruction of the PRD: inventing one would
be a guess presented as a source document.
