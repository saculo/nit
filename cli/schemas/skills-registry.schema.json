{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nit.dev/schemas/skills-registry.schema.json",
  "title": "Skills Registry",
  "description": "Custom skills available to routing beyond the base step skill and the module's language. Module-scoped skills live on the module itself, in boundaries/modules.json — this file is the global layer.",
  "type": "object",
  "properties": {
    "globalCustomSkills": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "id": { "type": "string" },
          "path": { "type": "string" },
          "description": { "type": "string" }
        },
        "required": ["id", "path"]
      },
      "description": "Custom skills available globally across all modules."
    }
  },
  "required": ["globalCustomSkills"],
  "additionalProperties": false
}
